import 'package:flutter/material.dart';

class NovelManagementScreen extends StatelessWidget {
  const NovelManagementScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
         child: Text('소설 관리 페이지')
      ),
    );
  }
}
